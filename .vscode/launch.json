{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Session Deck",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "X:\\demo-projects\\acme-storefront"
      ],
      "outFiles": ["${workspaceFolder}/out/**/*.js"],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
