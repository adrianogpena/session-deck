{
  "name": "session-deck-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/core",
    "packages/vscode",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm test -w @session-deck/core",
    "lint": "eslint packages/core/src packages/vscode/src packages/cli/src",
    "package:vscode": "npm run package -w session-deck",
    "sdeck": "npm start -w session-deck-cli"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.11.0",
    "eslint": "^10.11.0",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.70.1"
  }
}
